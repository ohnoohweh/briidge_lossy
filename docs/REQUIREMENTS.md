# Requirements

This document captures black-box requirements for the project. These are intentionally phrased as observable behavior, not implementation detail.

These statements are limited to project-owned behavior. They are not a catalog of end-user goals, deployment recipes, infrastructure prerequisites, operating-system capabilities, browser guarantees, or third-party library contracts. Those boundaries are described in [SYSTEM_BOUNDARY.md](/home/ohnoohweh/quic_br/docs/SYSTEM_BOUNDARY.md).

## Scope

ObstacleBridge is expected to:

- establish overlay connectivity between peers over supported transports
- carry UDP and TCP application traffic, plus Linux, Windows, macOS, and iOS TUN packet traffic, across that overlay where the required OS capability is available
- support listener and peer-client deployment modes
- expose runtime state and configuration through the admin web interface
- remain testable under reconnect, restart, concurrency, and lossy-path scenarios

The motivating user use-cases and the external assumptions around them are documented separately in [SYSTEM_BOUNDARY.md](/home/ohnoohweh/quic_br/docs/SYSTEM_BOUNDARY.md) and the user-facing sections of [README.md](/home/ohnoohweh/quic_br/README.md). The requirement IDs below describe what the project itself is expected to do inside that broader system context.

Secure-link authentication and encryption work now has three delivered requirement slices in this document:

- active `REQ-AUT-*` items for the delivered and defended PSK-based Phase 1 runtime slice
- active `REQ-AUT-*` items for the delivered certificate-based Phase 2 trust model and validation slice
- active `REQ-AUT-*` items for the delivered Phase 3 operational-control slice around live certificate/revocation reload and enforcement

The detailed realization concept remains in [SECURE_LINK_DESIGN.md](/home/ohnoohweh/quic_br/docs/SECURE_LINK_DESIGN.md), and the component ownership boundary remains in [ARCHITECTURE.md](/home/ohnoohweh/quic_br/docs/ARCHITECTURE.md).

Security controls that span multiple requirement areas, including WebAdmin authentication, guarded config writes, encrypted persisted secrets, controlled secret reveal, and threat scenarios, are summarized in [SECURITY_DESIGN.md](/home/ohnoohweh/quic_br/docs/SECURITY_DESIGN.md).

## Runtime compatibility requirements

- `REQ-RUN-001`: ObstacleBridge shall maintain Python 3.8 compatibility for Synology DSM deployments. Runtime code shall avoid syntax and import-time evaluated type expressions that require Python 3.9 or newer. The package metadata shall declare this support with `requires-python = ">=3.8"` in [pyproject.toml](/home/ohnoohweh/quic_br/pyproject.toml).
- `REQ-RUN-002`: The repository shall provide a buildable first-pass Synology DSM `.spk` wrapper for the Python runtime. That wrapper shall package the existing Python-first payload rather than a separate native Synology application, include the shared ObstacleBridge application icon in the generated SPK metadata, mark the package as beta until the Synology deployment path reaches a final release, emit a DSM-acceptable SPK container layout with one top-level `INFO`, one `package.tgz`, and the expected lifecycle metadata files without duplicate archive members, seed a default persistent config location for DSM-managed installs, declare the maintained Synology interpreter dependency on SynoCommunity `python314`, bootstrap the required Python runtime packages into package-managed storage for that interpreter, and provide DSM service lifecycle hooks that can start, stop, and report the package service while failing clearly when the maintained Synology Python runtime or required Python packages are unavailable. The DSM service wrapper shall also honor the bridge runtime's dedicated restart exit codes so operator-triggered restarts remain under DSM service control instead of being treated as a final stop. The same first-pass wrapper may also prototype a helper-oriented privilege split by preparing a dedicated helper interpreter copy for helper-mode launches, by prestarting package-user helper handoff metadata and socket state through DSM lifecycle hooks, by passing package-written helper socket/token metadata into the runtime through `OBSTACLEBRIDGE_PRESTARTED_TUN_HELPER_CONFIG`, and by pointing the existing Linux helper subprocess at that copy when a package-managed helper executable override is available. On current installable DSM 7.x packages, that helper-handoff prototype shall remain package-user-only unless a Synology-signed or development-token path is available, so the SPK shall not require an install-blocking root-privileged package declaration.
Implementation note: the current Synology service runner keeps shell `errexit` enabled for the wrapper as a whole, but must temporarily disable it around the bridge subprocess invocation so restart exit codes `75` and `77` can be captured and translated into immediate or delayed wrapper-managed relaunch instead of terminating the DSM service loop early.

## Overlay and transport requirements

- `REQ-OVL-001`: A peer client shall be able to establish a native UDP (`myudp`) overlay session to a listener and carry UDP application traffic across it.
- `REQ-OVL-002`: A peer client shall be able to establish a native UDP (`myudp`) overlay session to a listener and carry TCP application traffic across it.
- `REQ-OVL-003`: A peer client shall be able to establish a TCP overlay session to a listener and carry UDP application traffic across it.
- `REQ-OVL-004`: A peer client shall be able to establish a WebSocket overlay session to a listener and carry UDP application traffic across it.
- `REQ-OVL-005`: A peer client shall be able to establish a QUIC overlay session to a listener and carry UDP application traffic across it.
- `REQ-OVL-006`: Supported overlay transports shall work on both IPv4 and IPv6 where the specific transport mode is configured for that address family. For WebSocket listener mode, binding to the IPv6 wildcard address `::` shall create a dual-stack listener that accepts both IPv6 and IPv4-mapped incoming connections where the operating system supports dual-stack IPv6 sockets; this wildcard bind is an intentional operator-selected all-interface overlay listener, and operators who need narrower exposure shall bind a specific local address instead. Binding to `0.0.0.0` remains IPv4-only.
- `REQ-OVL-007`: Overlay peer resolution shall behave deterministically for reconnect scenarios on both IPv4 and IPv6. The runtime configuration surface shall scope peer-family preference per transport (`udp_peer_resolve_family`, `tcp_peer_resolve_family`, `ws_peer_resolve_family`, `quic_peer_resolve_family`), and peer endpoint fields shall accept either one host/FQDN or an ordered comma-separated list of IPv4/IPv6 alternatives. When multiple alternatives are configured, the runtime shall prefer addresses that match the selected transport-local family policy, but if the preferred family cannot be used and a usable alternative family is available, connection bootstrap shall fall back to that alternative instead of failing solely because the first family choice was unavailable.
Implementation note: operator-facing peer snapshots for peer-client transports shall distinguish the configured candidate set from the concrete endpoint that is actually in use. When a transport resolves or rotates through a comma-separated host list, `/api/peers` and the WebAdmin peer row shall report the applied peer host and port for the active attempt or live connection so the observed address family and selected candidate are unambiguous. When no live/applied endpoint has been learned yet, wrapped client snapshots may temporarily fall back to the configured target string until resolution or failover selects a concrete peer.

Implementation note: the delivered `myudp` peer-client runtime now keeps the ordered configured peer candidates on both the Python bridge path and the native Swift packet-tunnel bridge path, retries the next candidate after a short no-liveness window when the initially preferred family does not answer, retains the preferred IPv6 candidate through immediate hard local send errors in `prefer-ipv6` mode so transient route/socket readiness does not demote IPv6 too early, and normalizes IPv4 fallback onto dual-stack IPv6 sockets where needed so mixed-family `udp_peer` lists do not stall on the first address choice alone. When `prefer-ipv6` is configured but the selected peer target is a native IPv4 literal, both Python and Swift keep that candidate as native IPv4 at resolution time and apply the `::ffff:<ipv4>` form only as an IPv6-socket send normalization detail, so route exclusion and peer identity logic can continue to reason about the original IPv4 host. The delivered 2026-06-04 mixed Swift/Python `ws`/`quic` follow-up also hardens the native Swift service path used by `REQ-OVL-004` and `REQ-OVL-005`: WebSocket overlay application frames now carry an explicit overlay app-kind marker on both send and receive paths, outbound WebSocket messages are serialized through one queue instead of overlapping unsafely, QUIC transport-owner adapter frames now flow through the same transport send helper as ordinary overlay payloads, mixed host-side iOS plus macOS tests now cover larger TCP own-service carriage across Python peers for both `ws` and `quic`, and the current native Swift QUIC path keeps a documented large-write chunking workaround in place rather than leaving the macOS QUIC service case red.
Implementation note: the delivered follow-up for ordered peer lists now keeps permanent candidate rotation aligned across Python and Swift for `myudp`, `tcp`, `ws`, and `quic`. When the currently selected peer candidate does not become live, the runtime advances to the next configured candidate instead of probing each candidate once and then waiting for the outer restart watchdog to expire. The same slice also keeps the native Swift `tcp` and `ws` owners on per-transport peer-family resolution settings (`tcp_peer_resolve_family`, `ws_peer_resolve_family`) so reconnect/failover decisions stay aligned with the Python runtime.

## WebSocket proxy and payload requirements

- Functional decomposition note: the requirements in this section are implemented jointly by project-owned websocket bootstrap logic and dependency-owned websocket protocol handling. The ownership boundary is documented in `docs/ARCHITECTURE.md` under `WebSocket proxy tunneling`.

- Implementation note (Windows Negotiate path): the Windows WebSocket proxy `Negotiate`/NTLM auth path depends on `ctypes`/`wintypes` bindings for WinHTTP and SSPI calls inside the transport module; that import-time dependency is part of the delivered behavior for `REQ-WSP-009` and `REQ-WSP-012` failure reporting.

- `REQ-WSP-001`: A WebSocket peer client shall be able to establish its outbound websocket transport through an HTTP proxy when proxy routing is required for the target environment.
- `REQ-WSP-002`: The WebSocket proxy capability shall be scoped to peer-client mode only; it shall not imply listener-side proxy support.
- `REQ-WSP-003`: The WebSocket proxy capability shall be scoped to the WebSocket transport only; it shall not imply equivalent support for `myudp`, `tcp`, or `quic`.
- `REQ-WSP-004`: When proxy tunneling is enabled for the WebSocket peer client, the transport bootstrap shall establish the proxy tunnel before the websocket handshake begins.
- `REQ-WSP-005`: When proxy discovery, proxy connection, or proxy authentication fails, the WebSocket peer client shall report a connection failure without corrupting the overlay state machine.
- `REQ-WSP-006`: On Windows, the default WebSocket peer-client behavior shall honor the effective system proxy configuration unless the application configuration explicitly overrides it. When system proxy discovery returns no proxy endpoint for the target, that result shall be treated as a direct-connect path rather than as a fatal bootstrap error. When `ws_peer` is an ordered comma-separated host list, the selected resolved peer candidate shall be used for system proxy/PAC lookup, proxy CONNECT targeting, and direct preflight host handling instead of passing the raw host list as a URL authority.
- `REQ-WSP-007`: On Linux and other POSIX-style environments, the default WebSocket peer-client behavior shall honor the effective `HTTP_PROXY`, `HTTPS_PROXY`, and `NO_PROXY` environment settings unless the application configuration explicitly overrides it.
- `REQ-WSP-008`: Application configuration shall be able to consciously override the platform-default proxy behavior, including forcing direct connection or using an explicitly configured proxy endpoint.
- `REQ-WSP-009`: A WebSocket peer client running on Windows shall be able to establish its outbound websocket transport through an HTTP proxy that requires `Negotiate` / NTLM-style authentication.
- `REQ-WSP-010`: The WebSocket overlay transport shall support selectable payload transfer forms through `ws_payload_mode`: raw binary websocket frames (`binary`), plain base64 text websocket frames (`base64`), compact JSON text websocket frames carrying the base64 payload in the `data` field (`json-base64`), and a grouped semi-text form (`semi-text-shape`) using the alphabet `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-+`. Receivers configured for the text-oriented modes shall continue to accept raw binary websocket frames so mixed peers can fail soft during migration or debugging. A peer client shall advertise its configured payload transfer form during the WebSocket upgrade request, and the listener shall adopt that transfer form for the accepted peer automatically so compatible client/listener pairs do not require manually mirrored `ws_payload_mode` settings. For all text-oriented payload modes, the runtime shall budget WebSocket receive/open limits against the encoded frame size rather than the raw overlay payload size so text-mode expansion does not make healthy forwarded-service traffic fail solely because the websocket frame exceeds the raw payload budget.
- `REQ-WSP-011`: On the direct non-proxied WebSocket peer-client path, transport bootstrap shall complete a separate `GET /` HTTP preflight against the target listener before the later websocket upgrade attempt, shall consume the full HTTP response body for that preflight, and shall refuse the websocket upgrade attempt when the preflight status is not `200 OK`.
- `REQ-WSP-012`: When a WebSocket peer-client connection attempt fails during transport bootstrap or websocket opening, including failures such as DNS resolution, proxy negotiation, opening the HTTP preflight channel, unsuccessful `GET /` preflight, incomplete preflight body download, or websocket-open failure after bootstrap, the admin status shall report the connection as `FAILED` and expose a transport-level failure reason/detail until a later successful connection clears that failure state. Timeout-driven cleanup for aborted proxy-bootstrap helper tasks shall be treated as expected cancellation rather than surfacing a separate unhandled asyncio callback exception.
- `REQ-WSP-013`: Python and native Swift WebSocket peer clients shall accept an optional ordered `ws_peer_addresses` list containing only IPv4/IPv6 literals. When that list is non-empty, connection attempts shall rotate through those literal addresses without resolving `ws_peer`, while `ws_peer` remains the WebSocket URL authority, HTTP `Host`, TLS SNI server name, and certificate-verification name; automatic full-tunnel underlay exclusions shall likewise derive from the literal addresses without resolving the logical host. When `ws_peer_addresses` is omitted or empty, the runtime shall resolve `ws_peer` and retain the pre-existing DNS-backed connection behavior. The shared native Swift owner used by iOS and macOS shall use a Network.framework WebSocket connection for the address-override path so the physical endpoint can remain independent from the logical HTTP/TLS identity.

## Explicit proxy-provider requirements

- `REQ-PRX-001`: On iOS packet-tunnel providers, ObstacleBridge may run a first-party Swift local proxy provider for explicit proxy clients without embedding a Python proxy runtime in the Network Extension. The same proxy protocol behavior shall have a Python runtime equivalent for parity validation and non-extension use. The initial proxy provider shall support HTTP absolute-form requests, HTTP `CONNECT`, optional HTTP Basic proxy authentication, SOCKS5 method negotiation, SOCKS5 username/password authentication, SOCKS5 TCP `CONNECT`, IPv4/IPv6/domain SOCKS5 destination forms, bidirectional byte tunneling after connect, lifecycle start/stop ownership by the active runtime, and operator-visible listener/counter/error snapshots. On Python runtimes, the proxy-provider egress policy defaults to system proxy resolution for outbound provider traffic, reusing the same CONNECT tunnel path as the WebSocket peer-client proxy bootstrap; system mode uses WinHTTP/PAC/manual endpoint resolution on Windows and `HTTP_PROXY`, `HTTPS_PROXY`, and `NO_PROXY` environment resolution on Linux/POSIX. When that resolver returns no proxy endpoint, provider egress falls back to direct connection for that destination. SOCKS4, SOCKS5 `UDP ASSOCIATE`, transparent TCP interception, PAC serving, HTTP caching, content rewriting, and HTTP/2 or HTTP/3 proxy semantics are outside the first provider slice.

Current payload-form note:

- `binary`: the overlay wire bytes are sent directly as websocket binary frames with no text envelope.
- `base64`: the same overlay wire bytes are base64-encoded into one websocket text frame.
- `json-base64`: the overlay wire bytes are base64-encoded and emitted as compact JSON text of the form `{"data":"..."}`.
- `semi-text-shape`: the overlay wire bytes are split into 6-bit symbols using the alphabet `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-+`, the final symbol is padded with `0` bits if needed, and the resulting text is grouped into whitespace-separated runs of 1 to 8 symbols to keep a text-shaped appearance while remaining exactly reversible back to the original byte stream.

## Authentication and encryption requirements

This section covers the delivered PSK-based Phase 1 secure-link slice and the delivered Phase 2 certificate-based follow-up.

Functional decomposition note:

- `REQ-AUT-001` through `REQ-AUT-003`, `REQ-AUT-005`, `REQ-AUT-006`, and `REQ-AUT-007` are realized jointly by the secure-link runtime slice in [ARCHITECTURE.md](/home/ohnoohweh/quic_br/docs/ARCHITECTURE.md) (`ARC-CMP-006`), with lifecycle/config/snapshot wiring contributed by the runner/process orchestration layer (`ARC-CMP-004`).
- `REQ-AUT-004`, `REQ-AUT-008`, `REQ-AUT-009`, and `REQ-AUT-010` are realized jointly by:
  - the secure-link runtime slice (`ARC-CMP-006`), which owns the underlying authentication/encryption state and failure categories
  - the runner/process orchestration layer (`ARC-CMP-004`), which gathers and shapes snapshot data
  - the admin web and observability layer (`ARC-CMP-005`), which exposes aggregate runtime summary through `/api/status`, peer-scoped secure-link state through `/api/peers`, the live admin feed, and the WebAdmin page
- `REQ-AUT-011` through `REQ-AUT-019` are realized jointly by:
  - the secure-link layer (`ARC-CMP-006`), which owns the underlying authentication/encryption state and failure categories
  - the runner/process orchestration layer (`ARC-CMP-004`), which gathers and shapes snapshot data
  - the admin web and observability layer (`ARC-CMP-005`), which exposes aggregate runtime summary through `/api/status`, peer-scoped secure-link state through `/api/peers`, the live admin feed, and the WebAdmin page
- `REQ-AUT-020` is realized jointly by:
  - the compression layer (`ARC-CMP-007`), which applies per-frame compress-or-bypass and decode semantics
  - the runner/process orchestration layer (`ARC-CMP-004`), which wires peer-session wrappers and per-peer compression parameters
  - the admin web and observability layer (`ARC-CMP-005`), which exposes peer-scoped compression telemetry in `/api/peers`

The component ownership boundary for these secure-link requirements is documented in [ARCHITECTURE.md](/home/ohnoohweh/quic_br/docs/ARCHITECTURE.md).

The certificate/profile details that ObstacleBridge expects as input are documented in [SYSTEM_BOUNDARY.md](/home/ohnoohweh/quic_br/docs/SYSTEM_BOUNDARY.md), because they describe requirements on supplied key material and crypto support rather than black-box delivery of the current runtime.

Current implementation note:

- delivered secure-link modes now include `secure_link_mode=psk` and `secure_link_mode=cert`
- both delivered modes run on `overlay_transport=myudp`, `tcp`, `ws`, and `quic`
- the current runtime keeps mux payload budgeting aligned with the wrapped transport session budget for `myudp`, `tcp`, and `quic`, so SecureLink wrapping does not reduce healthy forwarded application traffic to the mux-header size alone
- when a protected client observes a transport-epoch change during reconnect or restart recovery, it now restarts the secure-link client handshake against that fresh transport epoch instead of continuing on stale client-side handshake state
- when a protected client is reconnecting on a fresh transport epoch, operator-visible connection state is layered rather than collapsed into a single boolean: the lower overlay transport may already be connected while SecureLink is still handshaking, so transport-connected visibility must remain true until that lower layer actually drops even when `app_ready` remains false and protected traffic is not yet eligible to flow
- when a peer-client lower transport remains up but the top protected layer never reaches `app_ready`, reconnect and candidate-rotation policy shall still treat that path as unavailable after the configured grace window, so SecureLink handshake failure or stall does not pin ChannelMux on a transport-only "connected" state indefinitely
- when a protected WebSocket client reconnects onto a fresh transport epoch, the secure-link client now preserves the restarted handshake across stale disconnect callback ordering and the runner's immediate transport-epoch reset callback, so the fresh session can authenticate instead of falling back to a persistent disconnected state during reconnect recovery
- when a protected WebSocket client reconnects onto a fresh transport epoch, the inner WebSocket transport now clears its prior connected-edge latch before the replacement socket's first RTT/PONG cycle so wrapper sessions such as SecureLink observe a fresh connected transition for the new epoch instead of inheriting the old socket's connected state and remaining stuck disconnected after a successful reconnect
- when a `myudp` transport epoch changes during reconnect or outage recovery, the runtime now clears stale sender state, receiver gap/missing state, queued datagrams, and control pacing state before the fresh SecureLink handshake begins, so old-epoch recovery feedback cannot leak into the new authenticated session
- when a WebSocket protected client reconnects after a previous transport epoch, it drops early-buffered protected frames from the old transport/session before accepting the fresh socket so stale old-session ciphertext is not flushed into the new SecureLink handshake
- the shared overlay integration harness now waits for TCP and WebSocket listener readiness before launching the peer client, so IPv6 direct-listener startup remains deterministic under the CI-aligned `pytest -q -n 16` xdist lane instead of depending on fixed sleeps between subprocess launches
- the mixed Swift/Python macOS host-runner overlay harness now compiles against the same native Swift source set as the packaged macOS app and waits for authenticated peer/readiness transitions before asserting forwarded traffic, so parity checks fail on true runtime drift rather than on compile-list skew or startup races
- mixed Swift/Python host-runner overlay cases in the shared integration harness are now explicitly macOS-only because they compile the macOS-oriented Swift host runner; the Linux shared lane keeps validating OS-independent Python overlay behavior instead of failing on unsupported Swift host-side compilation, and the empty-config onboarding admin regression is covered with a same-host `127.0.0.1` admin-port reservation so the harness probes the same loopback address the subprocess binds
- the authenticated WebAdmin certificate-reload browser-path integration case now gives its initial prime probe a longer startup budget on the Linux shared lane so slower CI runners can finish cert-mode first authentication before probe traffic is attempted; this is a testability hardening change for the existing `REQ-AUT-015`, `REQ-AUT-017`, and `REQ-AUT-019` contract rather than a behavior change in the delivered runtime
- the same authenticated WebAdmin certificate-reload browser-path integration case now also waits for the client-side authenticated admin session to expose both `CONNECTED` runtime status and an authenticated cert-mode secure-link peer row before attempting that first prime probe; this further narrows the Linux shared flake window around post-login session readiness without changing the delivered runtime contract
- the same authenticated WebAdmin certificate-reload browser-path integration case now also waits for the TCP probe listener to begin accepting connections before attempting that first prime probe, which removes another Linux shared race between successful cert authentication and final forwarded-service listener bring-up without changing delivered runtime behavior
- the same authenticated WebAdmin certificate-reload browser-path integration case now treats that first prime probe as a bounded readiness phase: when the client self-restarts or the cert session is still finishing startup, the harness refreshes the live client/server processes, re-establishes the authenticated admin session, re-validates the client-side authenticated cert row, and retries the initial prime probe inside a fixed window before the reload-button assertions proceed; this keeps the Linux shared lane aligned with the delivered recovery behavior without changing the runtime contract
- the cert-mode full-material reload integration case now also treats a brief post-reload admin-surface bounce as part of the accepted recovery window: it waits for the client admin endpoint to come back and for the client runtime to report `CONNECTED` again before asserting the post-reload authenticated peer state, which keeps the Linux shared lane aligned with the delivered reconnect/re-auth behavior instead of failing on a transient `Connection refused`
- secure-link failures report a disconnected lifecycle state without owning a lower-transport reconnect; ChannelMux requests one cascaded rotation after 30 seconds of continuous outer-layer disconnection and waits for a newer lifecycle epoch before another request, while the client restart watchdog remains independent of SecureLink recovery status
- cert revocation reload keeps the stable operator contract on the aggregate `/api/status` reload scope/result and dropped-peer counters after the revoked peer has been disconnected; peer-scoped failed rows may be transient when the lower transport reconnects and the secure-link session object has already been torn down
- local server-owned TUN lifecycle hooks now receive the same peer/subnet metadata that the existing server hook scripts and docs already expect, including `PEER_ADDR`, `TUN_SUBNET`, `PEER_ADDR6`, and `TUN_SUBNET6`, so a local shared-TUN hook can assign interface addresses and forwarding/NAT rules without requiring manual `ip addr` or route setup
- the TUN-routing contract is now more centralized and consistent across Python and Swift: `TUN_routing` now serves as the single source of truth for generated hook defaults such as `MTU`, `ENABLE_TCPMSS`, optional TUN tcpdump controls, DNS, included-route lists, and peer/subnet metadata; explicit empty server-side `tunnel_gateway` / `tunnel_gateway6` values are preserved instead of silently reverting to defaults; Linux server-side TUN hooks no longer require `PEER_ADDR`; structured `tun -> tun` service entries may omit per-service `mtu` so they inherit `TUN_routing.mtu` unless a service-level override is intentionally supplied; and the effective local excluded-route set now auto-appends the resolved active overlay peer IPv4 and IPv6 addresses so full-tunnel clients keep the transport peer off the tunnel by default across the Python hook path and the Swift packet-tunnel / HostRunner path, with exact-route snapshotting on Linux so preserved local subnets stay attached to their original interfaces during full-tunnel setup
- broader multi-peer listener validation now exists on `ws`, `myudp`, `tcp`, and `quic`, with the deepest concurrent channel-routing slice still exercised on the TCP transport
- the runtime now exposes aggregate runtime metadata and secure-link reload/apply summaries through `/api/status`, while peer-scoped traffic, connection, compression, and secure-link observability live in `/api/peers` and `/api/connections`
- in `secure_link_mode=cert`, operators can now trigger live `revocation`, `local_identity`, or `all` reload/apply actions through `POST /api/secure-link/reload` and the WebAdmin `Reload Revocation`, `Reload Local Identity`, and `Reload All` controls without restarting the process
- after a cert revocation reload enforces a peer disconnect, `/api/status` and `/api/peers` retain the trust-enforcement disconnect reason, while restart-recovered myudp listener rows suppress superseded stale secure-link state before adding SecureLink-owned mux routing hints for peer-scoped traffic accounting
- some secure-link subprocess integration tests use a test-only failure-injection seam that is enabled explicitly by the harness and is not reachable in the normal runtime by default; this seam exists only to defend reconnect/replay/fail-closed requirements where pure black-box stimulation would otherwise add disproportionate harness complexity
- the current user-facing runtime configuration surface also includes `secure_link_root_pub`, `secure_link_cert_body`, `secure_link_cert_sig`, `secure_link_private_key`, `secure_link_revoked_serials`, and `secure_link_cert_reload_on_restart` for `secure_link_mode=cert`
- the Phase 1 PSK mode remains useful for development/testing/lab bring-up, while the certificate-based Phase 2/3 slices provide deployment-rooted mutual authentication, trust validation, and operator-driven live trust-material application

- Implementation note (instrumentation): The runtime now includes additional `secure_link`-scoped DEBUG logging that surfaces incoming secure-link frames and handshake events to aid troubleshooting WebSocket-secure-link handshakes. Operators may enable `secure_link` DEBUG logging temporarily when diagnosing handshake/transport interactions. (see PR #178)
- Implementation note (instrumentation): ChannelMux UDP service forwarding now has sampled `channel_mux` DEBUG breadcrumbs for the local-to-overlay, overlay-to-target, target-to-overlay, and overlay-to-local path directions. The breadcrumbs are intentionally DEBUG-only so operators can troubleshoot one-way UDP service traffic without cluttering normal INFO logs.
- Implementation note (testability): The delivered integration harness now generates localhost TLS fixture material at runtime and allocates loopback port blocks by probing host availability before selecting a case slot. This keeps localhost private keys out of version control and preserves stable Linux shared integration coverage even when unrelated host daemons already bind uncommon local ports.

- `REQ-AUT-001`: The project shall provide one transport-independent PSK secure-link capability for overlay authentication and protected data carriage across `myudp`, `tcp`, `ws`, and `quic`.
- `REQ-AUT-002`: When both peers are configured with the same PSK, the secure-link protected data phase shall authenticate successfully before overlay traffic is accepted and forwarded. On the listener/server side, authentication shall complete as soon as the client proof-of-key-possession frame is decrypted; it shall not wait for a first real application payload before reporting the session as authenticated.
- `REQ-AUT-003`: When peers are configured with different PSKs, the protected data phase shall not start, overlay traffic shall not be forwarded, and the session shall remain observable as an authentication failure rather than a false connected state.
- `REQ-AUT-004`: The admin web interface and admin API shall expose secure-link state in an operator-usable way: peer-scoped secure-link state shall be reported with the corresponding peer rows and peer views so an operator can distinguish disabled, handshaking, authenticated, and failed protected-overlay states, including the reported authentication failure category, while the peer box preserves connection uptime and transport-appropriate protocol statistics. On secure-link-wrapped `myudp` listener and peer rows, `/api/peers` shall continue to report the underlying `myudp` frame/transmit counters for the corresponding active peer after protected traffic has flowed; the wrapper must not collapse those counters to zero.
- `REQ-AUT-005`: Multi-peer listener scenarios using secure-link PSK shall preserve peer isolation and distinct authenticated-peer visibility across the supported listener transports, with the deepest concurrent routing slice defended on the TCP listener path. SecureLink-wrapped listener transports shall keep encrypted handshake, proof, and data frames byte-for-byte intact until the SecureLink layer authenticates/decrypts them, even when those protected bytes resemble channel-mux frames. The integration harness shall require authenticated secure-link peer state before expecting client-published TCP services to accept probes in those secure multi-peer slices. On mixed-listener processes that expose both WebSocket and non-WebSocket overlay transports, repeated plain HTTP handling on the WebSocket front end shall remain isolated from healthy secure-link peers on either transport. On WebSocket listener peers wrapped by SecureLink, per-peer mux ownership shall keep `/api/peers` traffic counters scoped to the correct authenticated peer rather than reporting the same aggregate traffic on every peer row.
- `REQ-AUT-006`: The PSK secure-link runtime shall preserve a deterministic session and counter lifecycle: protected data counters start at `1`, remain strictly monotonic per direction, reject stale or reserved counters, and when `secure_link_rekey_after_frames` is configured to a positive value the runtime shall rotate to a fresh secure-link session under live traffic without losing healthy overlay functionality. WebSocket reconnect recovery shall install a fresh secure-link session lifecycle without carrying protected frames encrypted under the prior session into the new transport epoch. When the lower transport remains established while SecureLink is rotating or re-establishing its authenticated epoch, the exposed secure-link state shall report that top layer as `reauthenticating` rather than as a healthy authenticated path.
- `REQ-AUT-007`: Malformed, unexpected, or out-of-order secure-link control/data frames shall fail closed: the affected secure-link peer state shall stop forwarding overlay traffic, remain observable as a secure-link failure, and drop any server-side peer-channel routing state that belonged to the failed peer. Transport reconnect handling shall drop stale protected frames known to belong to an old secure-link session before they can be interpreted as unexpected input on the fresh session. A client-side secure-link session that has only locally validated `server_hello` but has not yet reached peer-confirmed protected traffic shall also fail closed if that unconfirmed handshake state exceeds the configured lifecycle timeout, rather than remaining indefinitely half-authenticated.
- `REQ-AUT-008`: Repeated client-side PSK authentication failures shall be retried with bounded backoff rather than an immediate tight loop, and the peer-scoped admin/API surface shall expose the current consecutive failure count and next retry window so operators can diagnose persistent secret mismatch or similar authentication failures. When an already-authenticated client-side SecureLink session fails closed, it shall report `Disconnected` to the immediate outer layer and wait for ChannelMux's lifecycle rotation policy to request a new lower-layer connection epoch. Handshake-timeout failures that occur before peer-confirmed authentication shall stay on the bounded retry path instead of being treated as successful authentication.
- `REQ-AUT-009`: The peer-scoped admin/API surface shall expose the most recent SecureLink event, handshake-attempt count, authenticated-session count, completed-rekey count, last authenticated timestamp, most recent failed session id, connection uptime, peer-scoped passed-frame and dropped-frame counters, and peer-scoped traffic counters/rates so operators can distinguish repeated authentication failures, live-session rotation, and per-peer listener activity. The reported lifecycle status shall be `Connected` only after peer-confirmed SecureLink authentication and `Disconnected` for handshake or secure-link failures. In particular, an initial client that has locally validated `server_hello` and sent its protected proof shall remain `Disconnected` until the peer's protected acknowledgement completes that handshake; ChannelMux shall not open the outer connection during that local-only phase. This supported operator surface is intentionally summary-oriented: it does not require per-frame handshake-debug telemetry, plaintext telemetry payload reflection, or other debug-only protocol internals in the normal WebAdmin or admin-API contract.
- `REQ-AUT-010`: The PSK secure-link runtime shall support time-based rekey on authenticated client-side sessions and operator-forced rekey through the admin API and WebAdmin controls; both paths shall rotate to a fresh secure-link session without breaking healthy overlay traffic and shall remain observable through peer-scoped admin/API fields that identify the last rekey trigger and any scheduled rekey deadline. Time-based rekey shall arm from the authenticated client-side session itself, shall not require a later protected application frame before the timer starts, and shall not be postponed indefinitely merely because protected traffic continues to flow. After a time-based rekey completes, the authenticated session shall remain stably observable as rekey-complete rather than immediately re-entering a fresh time-triggered rekey cycle without a newly established authenticated session, and that freshly authenticated post-rekey session shall arm a later time-threshold rekey again so long-lived healthy sessions continue rotating instead of stopping after one time-based rekey. Operator-triggered rekey requests shall target a specific peer row rather than acting as an implicit runtime-wide broadcast, and the commit-to-done cutover window shall preserve healthy overlay traffic rather than dropping same-channel application payloads solely because the server has already switched to the new session before the client receives `REKEY_DONE`. Python runtime rekey diagnostics shall record request, protocol-leg, timeout, and authentication-failure context with active/pending session ids, counters, trigger, pending age, and the wrapped protocol's transmit-delay estimate; the log shall expose the 120-second two-transfer rekey budget and whether that estimate exceeds it, without emitting protected payloads, keys, or nonces. The native Swift runtime keeps equivalent protocol behavior but has no process-log sink; this diagnostic output is intentionally Python-runtime scoped.

- `REQ-AUT-011`: The deployment trust anchor shall be an admin-controlled root public key configured on peer clients and peer servers, and `secure_link_mode=cert` shall authenticate successfully only when both peers trust the same deployment root.
- `REQ-AUT-012`: Peer certificates used by `secure_link_mode=cert` shall be issued by that deployment-local admin root and constrained by machine-enforced roles so that client/server direction mismatches fail closed before protected traffic starts.
- `REQ-AUT-013`: Certificate role checks, validity checks, deployment-scope checks, and serial-based revocation checks shall be enforced before the protected secure-link data phase is entered, and the failure shall remain observable as a secure-link authentication/trust failure rather than a false connected state.
- `REQ-AUT-014`: The certificate-based secure-link mode shall preserve the same peer-scoped admin/API visibility model as the current PSK slice while adding peer identity and trust-validation details such as subject id/name, roles, deployment id, serial, issuer, trust-anchor id, trust-validation state, and trust-failure diagnostics.
- `REQ-AUT-015`: The certificate-based secure-link runtime shall support operator-triggered live reload of revocation material from the configured revoked-serial source without requiring a process restart.
- `REQ-AUT-016`: After a successful live revocation reload, peers whose certificates are now revoked shall be disconnected immediately and shall remain observable as peer-scoped secure-link trust failures rather than silently continuing on superseded trust state.
- `REQ-AUT-017`: The certificate-based secure-link runtime shall support operator-triggered live reload of local root/certificate/private-key material from the configured files, and it shall validate the full replacement bundle atomically before activation so that a broken bundle does not partially replace the active material.
- `REQ-AUT-018`: After a successful live local-identity reload, already-authenticated certificate-mode peers shall not continue indefinitely on the superseded local identity; they shall be disconnected and required to re-authenticate under the new material generation on a fresh lower-transport epoch rather than continuing the superseded secure-link/transport epoch.
- `REQ-AUT-019`: The admin API and WebAdmin shall expose aggregate reload/apply results at the runtime level and peer-scoped enforcement/disconnect diagnostics at the peer level so operators can tell what changed, when it changed, and why a peer was dropped or re-authenticated.
- `REQ-AUT-020`: Compression-enabled secure-link peers shall interoperate even when client and server use different local compression settings (for example different `compress_layer_min_bytes`, `compress_layer_level`, and allowed `compress_layer_types`). The peer-client setting shall control whether a peer connection actively uses compression, and client-side peer rows shall report that configured compression state even before counters are nonzero. The peer server shall keep a passive decoder, detect valid client-compressed frames from the wire-level compression signal, and expose compression only for those activated peer rows. After such activation, the peer server shall compress replies for that peer without requiring mirrored local thresholds or levels, as long as both peers support the same compression framing and decode guardrails. Compression telemetry shall be peer-scoped, and emitted output-byte totals shall account for both compressed output and uncompressed emitted payload bytes when a compression attempt is skipped because it does not reduce size.

## Reconnect and restart requirements

- `REQ-LIFE-001`: When one side disconnects or is restarted, the remaining side shall eventually report the overlay as not connected.
- `REQ-LIFE-002`: When the disconnected side returns, the overlay shall reconnect automatically when the configured topology supports reconnection. Each accepted candidate rotation shall publish a newer disconnected connection epoch before retrying, so ChannelMux can continue its bounded rotation policy when the attempt does not recover. Python `myudp` clients and the native Swift UDP owner shall close the prior datagram endpoint and create a fresh endpoint before probing the selected peer, so the retry has the same socket-lifetime boundary as a process restart. Reconnect handling shall reject stale Python and Swift WebSocket callback activity from superseded sockets, and a fresh reconnect attempt shall not inherit stale RTT-connected state or allow overlapping client connect attempts to promote more than one active WebSocket at a time. Each Swift WebSocket connection attempt shall reset its local ChannelMux TUN state before task allocation. Once the authenticated overlay is ready, Python ChannelMux shall allocate and announce every configured local TUN listener, and Swift shall allocate and announce its configured local TUN endpoint, with `OPEN` before any local packet arrives so a shared-TUN peer never observes `DATA` without a binding. Python TCP, QUIC, and WebSocket reconnect-coroutine cleanup shall retain its task identity while the event loop is active so interpreter shutdown can close a suspended coroutine without querying an event loop that has already stopped. The integration harness shall treat expected self-restart relaunches as part of the bounded reconnect wait window so reconnect validation observes the fresh process instead of timing out against the prior deadline.
- `REQ-LIFE-003`: After reconnection, traffic forwarding shall resume and probes shall again succeed.
- `REQ-LIFE-004`: Restart-specific regressions for concurrent channel cases shall remain covered so existing functionality does not silently erode.
- `REQ-LIFE-005`: Repeated failed reconnect attempts shall be throttled by a configurable minimum retry delay so client overlays do not hammer connection setup continuously while a peer remains unavailable.
- `REQ-LIFE-006`: Operator-triggered reconnect requests exposed by the admin API and WebAdmin shall be scoped to the selected established peer connection rather than being process-global across unrelated peer sessions. When the selected client overlay path does not expose a transport-local reconnect hook but does expose a restart-based reconnect owner, the operator-triggered reconnect flow shall use that bounded runtime recovery path instead of failing as unsupported.
- `REQ-LIFE-007`: Startup through the default runtime entrypoint shall tolerate a missing or empty default config file by continuing with built-in defaults, while malformed JSON config input shall fail fast with a clear error.
- `REQ-LIFE-008`: When startup uses the default runtime entrypoint and Admin Web is enabled, the launcher shall print a clickable Admin Web entrypoint URL derived from the effective Admin Web bind/port/path configuration before handing control to the supervised bridge process. For wildcard/global Admin Web binds, the launcher may additionally print clearly labeled network-reachability hints derived from the local host and best-effort public address discovery, but those extra lines shall remain advisory rather than a guarantee of external reachability and may be emitted after the supervised bridge process has already started so slow public-address discovery does not delay local operator access. When the launcher is using its default redirected-child mode and the supervised bridge process exits nonzero during startup, the launcher shall replay any captured child stderr tail to the operator instead of failing silently.
- `REQ-LIFE-009`: When startup uses the default runtime entrypoint and required Python runtime packages are missing, the launcher shall identify the missing packages before starting the supervised bridge process, print the editable-install command for the active interpreter, and offer an interactive installation prompt when stdin is a terminal. Non-interactive startup shall not block on a prompt; it shall print the dependency guidance and continue so service managers and automation do not hang. WebAdmin onboarding shall expose equivalent runtime dependency health so first-run setup can warn operators before configuration persistence fails because a required package such as `cryptography` is unavailable.
- `REQ-LIFE-010`: The runtime shall expose an embeddable, process-entrypoint-independent control surface that can build effective runtime arguments from an in-memory configuration mapping, start and stop the existing runner from a host event loop, provide redacted status/config/connection snapshots, and reserve a packet-I/O abstraction that preserves packet boundaries for future platform TUN adapters. This embeddable surface shall remain usable from packaged hosts such as the iOS companion app and the standalone iOS E2E harness, where the runtime is started from JSON or in-memory configuration without invoking the normal Python process entrypoint. When an embedder provides a host-owned configuration file path, runtime configuration persistence shall target that path so packaged hosts can keep operator-edited config files in platform-appropriate storage such as the iOS Documents container. The shared runtime config surface may also carry a dedicated `tun_execution` section that describes future helper-backed local TUN topology independently from `TUN_routing` packet-addressing policy, so privilege-topology experiments can evolve without overloading route settings. The delivered Linux-first helper slice now permits `tun_execution.mode=helper` to start an authenticated local helper control channel, launch the helper module as a separate subprocess, expose helper status through runtime snapshots, route local `ChannelMux` TUN open/read/write traffic through either the helper-backed in-process Linux memory backend, a selectable native Linux helper backend that can own a real TUN fd, or the client-only path that talks to the helper subprocess, drive a helper-network apply/remove control path for helper-managed TUN open/close when `helper_apply_network=true`, and expose a runnable helper module entrypoint plus async serve contract for future privileged-helper launch work. The helper launch path now hands the subprocess a compact effective JSON config through a short-lived private file passed by `--config-path`, rather than expanding the helper session token and launch knobs directly on the process command line. For the native Linux helper backend, the launch path now prefers a direct subprocess start when the current Python executable or current process already provides `CAP_NET_ADMIN` or `CAP_SYS_ADMIN`, and falls back to helper-subprocess-only `sudo` only when that capability path is unavailable. On the delivered native Linux helper path, that helper-owned network work now includes interface IPv4/IPv6 tunnel-address programming, helper-merged excluded-route programming, non-default included-route programming, Linux policy-table full-tunnel default-route handling, helper-owned DNS apply/remove through `resolvectl` when that manager is available on the Linux host, helper-owned Linux server-side forwarding/NAT/TCPMSS firewall lifecycle when the helper payload carries listener-side `WAN_IF` intent, suppression of runtime listener-side TUN `on_created` / `on_stopped` host-network hooks so helper-managed TUN devices keep privileged host-network ownership in one place, structured runtime snapshot diagnostics for helper-network failures together with best-effort rollback of partially applied helper-owned state when `apply_network` fails mid-flight, elevated proof that such a native helper-side `apply_network` failure after partial route/address programming really does roll back those host mutations on the privileged path while preserving helper `last_failure` cleanup diagnostics, elevated proof that native helper death during `remove_network` after earlier cleanup steps can still leave helper-owned firewall state behind on the real privileged helper path, client-side snapshot refresh after asynchronous helper apply/remove failures so Admin Web and runtime status observe the helper-side failure record, runtime helper status that marks post-start helper socket loss as disconnected while preserving the last known helper runtime snapshot plus the helper connection error and process return code for diagnostics, a helper lifecycle phase field so operator surfaces can distinguish launching, socket-wait, client-connect, connected, failed, and stopping states, and a recovery warning plus repair hint when that last cached helper snapshot still suggests that helper-owned firewall or other host-network state may need manual cleanup before restart. On sudo-launched native-helper starts, the helper-owned Unix control socket shall also be handed back to the invoking desktop user's UID/GID so the unprivileged runtime can connect to the elevated helper instead of failing with a permission-denied socket connect. On the Linux-native path, the runtime may also use that same cached helper snapshot as a cleanup recipe for a manual operator-triggered repair flow that replays helper-owned route, firewall, DNS, and tunnel-address removal after the helper has already died, and elevated integration now proves that this repair flow can remove real surviving helper-owned firewall and underlay-route state after helper loss. Helper status now also preserves the most recent repair result together with a post-repair stale-state verdict and best-effort verification details from the tracked helper-owned addresses, routes, policy rules, firewall rules, and DNS state so Admin Web can distinguish a repair that cleared the reported stale helper-owned state from a repair attempt after which stale state may still remain. The Linux helper lifecycle now also covers abnormal parent-loss cleanup more explicitly: the helper shall stop itself after a short no-authenticated-client window, snapshots shall expose the authenticated-client count needed to diagnose that state, and runner startup may reap stale helper launch records whose helper process still exists but no longer has a real authenticated runtime client. The native Linux route-policy path shall also tolerate non-canonical `ip rule show` rendering when deciding whether helper-owned or host-owned policy rules already exist, including host routes printed without `/32` and default rules rendered without an explicit `to` clause. When helper mode is selected on Linux, the runtime shall suppress the old whole-process desktop `sudo` relaunch; if the selected helper backend needs privilege, only the helper subprocess shall be elevated with an operator-facing explanation before the password prompt when capability-based direct launch is not already available. The normal desktop inline backend remains available, and the remaining helper lifecycle work is now broader hardening around recovery and cleanup repair rather than an uncovered basic elevated helper lane. The default desktop Python entrypoint shall also preserve the same runner path when inline local TUN backends need operating-system privileges, including relaunching itself through `sudo` on Unix desktop hosts with an operator-facing explanation before the password prompt, and through the native privilege prompt on platforms such as Windows where WinTun adapter creation requires elevation.
Elevated TUN route and DNS verification shall assert the configured local host-network state and its Admin diagnostics without depending on an external host's ICMP policy. Dedicated packet-carry cases shall prove overlay data delivery separately. Elevated test wrappers shall emit unbuffered per-test start/result markers; the macOS wrapper shall additionally capture route, interface, and process diagnostics on exit so a hosted-runner interruption can be correlated with its last running case.

  The `tun_execution` section shall accept both concise configuration keys (`mode`, `helper_backend`, `helper_socket`, `helper_apply_network`, and `helper_log_level`) and their CLI destination names. A live bridge process identified by a helper launch record shall not be reaped solely because its helper has not yet authenticated a client.

  Native Darwin helper control requests shall allow sufficient time for privileged `utun` creation and activation before failing an `OPEN_TUN` request. Its runtime snapshot shall retain a bounded hook-execution history so operators and tests can verify required lifecycle hooks even after later TUN operations update the last-hook fields.

- `REQ-LIFE-011`: On iOS, operator-triggered restart and reconnect actions exposed through Admin Web, including restart-after-save flows, shall continue to work when the Network Extension is running without the ObstacleBridge app process. The restart owner for that runtime shall therefore be the extension itself rather than the foreground app.
- `REQ-LIFE-012`: When helper-owned TUN network application is active on a supported backend, WebAdmin shall expose a TUN / Routing control that suspends or enables only the configured included routes. Suspending shall remove those included routes while retaining the interface, tunnel addresses, excluded routes, DNS, and firewall state; enabling shall reapply the configured included routes. `TUN_routing.enabled_on_startup` shall control whether included routes are applied during startup. The control shall report its current and startup state, and shall be unavailable on a backend that cannot safely provide route-only toggling.

Lifecycle ownership: transports, SecureLink, and Compression publish typed lifecycle-event snapshots and report candidate-cycle exhaustion without restarting themselves. SecureLink reports authentication failure without initiating transport recovery. ChannelMux requests one cascaded rotation after 30 seconds of continuous disconnection, and it may immediately cascade an explicit operator material-reload rotation through Compression, SecureLink, and Transport. Raw transport connectivity does not reset the candidate-cycle budget; ChannelMux resets it only after the outer stack reports connected. Runner restarts once when a transport reports exhausted candidate cycles. Shared Swift UDP, TCP, WebSocket, and QUIC owners shall mirror this contract: outer app readiness gates ChannelMux and TUN ingress, lifecycle diagnostics include the active epoch and reason, and each owner shall request its existing candidate reconnect path after the delayed rotation signal while publishing restart-required after three exhausted cycles.

ChannelMux admits TUN packets, TUN reader registration, and internal connectivity probes only while the current lifecycle epoch is connected. A disconnected lifecycle event withdraws admission before local packet forwarding or probe injection can enter the mux path. Runner consumes an exhausted transport candidate-cycle result and requests one process restart with the transport and cycle diagnostic reason; its watchdog is reserved for sessions that have emitted no lifecycle state.

WebAdmin peer diagnostics separate connection endpoint/uptime state, protocol quality and traffic counters, ChannelMux open-channel counts and candidate/restart timers, and TUN throttle state. Transport, Protocol, and ChannelMux remain visible for every non-listening peer; enabled SecureLink and Compression layers remain visible while their reported state is disconnected, failed, or reconnecting.

Current lifecycle implementation note:

- the macOS TUN paths now cover both Python inline and helper-owned privilege boundaries. Inline mode keeps using service lifecycle hooks to run the Darwin TUN scripts around real `utun` devices, while the Darwin native helper path permits `tun_execution.mode=helper` with `tun_execution.backend=darwin-native`, opens real `utun` devices through the privileged Python helper, adapts Darwin `utun` address-family headers to the runtime's raw IP packet boundary, delegates helper-owned host-network apply/remove to the Darwin TUN hook scripts, reports the actual helper-created interface name back into runtime snapshots, carries real packets through the helper-backed overlay path, proves live Darwin route/DNS hook effects for helper-owned `utun` setup, reports helper death through Admin Web helper status, raises the manual-cleanup warning when cached helper runtime still suggests helper-owned route or DNS state may remain after helper loss, verifies kernel cleanup of the helper-owned `utun` interface and helper-managed routes after helper loss, and keeps macOS helper mode on the helper-subprocess elevation path rather than relaunching the whole desktop runtime. The macOS Swift host-runner path uses the same narrow helper responsibility split through an Apple-native packaged helper, `SMAppService` lifecycle/status actions, and XPC command/packet transport when the helper package is approved and reachable. Hosted GitHub macOS cannot grant the Swift host runner the permission required for real `utun` packet injection, so that one packet-carry integration case skips there while it remains required on authorized local macOS; hosted-runner tests otherwise treat `not_registered` / `helper service is not enabled` after registration preflight as an environment approval boundary, and loopback-shaped helper transport remains available for local testing and unapproved helper packages.
- the Windows helper path now extends the same helper-owned privilege boundary to `tun_execution.mode=helper` with `tun_execution.backend=windows-native`. The runner can launch the helper through an elevated Windows process when the desktop runtime is not already Administrator, use loopback TCP or Windows named-pipe style helper endpoints instead of Unix sockets, keep longer control-plane timeouts for Windows helper startup, and reap stale helper launch records even when the endpoint is not represented by a filesystem socket. The selectable Windows-native backend wraps the existing WinTun adapter seam for helper-owned packet read/write, applies helper-owned IPv4/IPv6 addresses, included/excluded routes, DNS, and first-slice Windows Defender Firewall state through PowerShell-backed host control, rolls back partial apply failures, and can replay plus verify cached route/address/DNS/firewall cleanup after helper loss. Elevated Windows coverage now exercises helper-owned network apply, packet carry, helper-death warning reporting, and Admin-triggered stale-state repair on the real WinTun path; firewall lifecycle remains unit-covered until a dedicated elevated firewall integration lane is added.
- the default launcher subprocess now invokes `from obstacle_bridge.bridge import main; main()` instead of `-m obstacle_bridge.bridge` so repo-root shim imports and split transport modules do not execute the runtime module twice under different names during supervised startup; the same recovery slice also restores the test-only failure-injection runtime wiring used by secure-link replay/fail-closed integration coverage and broadens the iOS Documents-root fallback to tolerate general OS-level write errors at import time
- the host-side iOS E2E runtime-contract lane now relies on worker-partitioned local TCP/UDP test-port allocation so parallel xdist execution does not lose preselected WebSocket, admin, or UDP service ports before the embeddable runtime actually binds them
- the iOS embedded-runtime restart path now restarts the Python stack from the host-owned event loop, rereads the grouped configuration file before starting the replacement runtime, and bounds shutdown so a slow AdminWeb listener close or lingering client sockets cannot stall restart for minutes; the packaged WebAdmin frontend correspondingly treats embedded restart as a recovery flow rather than assuming a separate process exit/relaunch cycle
- the iOS Network Extension restart path now owns restart/reconnect and restart-after-save handling inside `PacketTunnelProvider`, so Admin Web restart flows do not depend on the ObstacleBridge app process being alive when the extension is already running
- the default runtime entrypoint now logs process-start, signal, shutdown-request, restart-request, runner-stop, mux-stop, and process-exit breadcrumbs with explicit `reason=` attribution so externally triggered termination, WebAdmin-driven shutdown/restart, watchdog-driven restart, and normal stop finalization can be distinguished from an abruptly truncated log. When `log_file_truncate_on_start` is enabled, startup also preserves the prior active log as `<log>.lastsession` before opening a fresh active file so operators can inspect the previous run while a new session is already writing.
- the default module launcher now preserves its quiet redirected-child path for healthy runs but replays the captured child stderr tail when the supervised bridge process exits nonzero during startup, so issues such as a root-owned `--log-file` path are still visible to an unprivileged operator. During delayed restart windows (`exit code 77`), that same launcher now temporarily keeps the Admin Web bind/port occupied with a small holding response that reports the remaining restart countdown instead of leaving the admin endpoint dark until the relaunched runtime binds again.
- the runner status snapshot now falls back to a minimal `peer_state` plus diagnostic marker if stats snapshot rendering raises, and Admin Web can derive the same minimal status from the live runner session when no cached status snapshot exists yet, so Admin status polling remains useful during startup/reconnect; macOS elevated test scripts also preserve the GitHub Actions marker through their sudo re-exec so hosted-runner-only diagnostics stay scoped to CI

## Listener and multi-peer requirements

- `REQ-LST-001`: A WebSocket listener shall support two independent peer clients concurrently.
- `REQ-LST-002`: A myudp listener shall support two independent peer clients concurrently.
- `REQ-LST-003`: A TCP listener shall support two independent peer clients concurrently.
- `REQ-LST-004`: A QUIC listener shall support two independent peer clients concurrently.
- `REQ-LST-005`: When a listener has multiple connected peers, the admin peer API shall report distinct peer endpoints for the connected peers. For shared-TUN listeners that maintain peer ownership/binding state, the dedicated TUN/routing admin surface shall also expose the currently bound peer together with the effective bound ownership IP addresses so operators can see which address claim is linked to the active binding.
- `REQ-LST-006`: Listener-side peer reporting shall distinguish passive listening state from active connected peers, keep the passive listener row zeroed, and expose live per-peer connection metrics such as RTT on the active accepted peer rows. Peer-scoped traffic totals shall remain attributable to the owning peer after individual UDP/TCP/TUN mux channels close, so short-lived service channels do not disappear from the peer-level accounting immediately after teardown.
- `REQ-LST-007`: Listener behavior shall remain peer-independent: auxiliary listener activity on a shared endpoint, including non-upgrade HTTP handling on a WebSocket listener, repeated plain HTTP requests on the same TCP connection before any later upgrade, per-peer handshake/failure handling, and disconnect cleanup, shall stay scoped to the originating request or peer and shall not degrade healthy traffic forwarding, published-service reachability, or authenticated session state that belongs to another peer, including a healthy peer that is using a different active transport on the same listener process.

## Mixed traffic and channel requirements

- `REQ-MUX-001`: A connected peer shall be able to carry multiple simultaneous TCP channels over one overlay connection.
- `REQ-MUX-002`: A connected peer shall be able to carry mixed UDP and TCP services at the same time.
- `REQ-MUX-003`: Multi-client listener scenarios shall preserve peer isolation so one peer’s channels and services do not conflict with another peer’s. Listener-side ChannelMux channel identity shall include both the owning peer and channel identifier, because independent peers may legitimately allocate the same channel number. A process-shared TUN device shall route local replies through the mux that actively owns its reader, using that reader owner's peer/channel bindings rather than creator or attachment history.
- `REQ-MUX-004`: Remote service publication shall remain scoped to the intended peer.
- `REQ-MUX-005`: Listener-side service, catalog, cleanup, and listener-start decisions shall be keyed to the owning peer or request scope rather than to process-global singleton state, so legacy single-peer assumptions cannot make unrelated peers inherit disconnects, catalog replacement, auxiliary-endpoint side effects, or duplicate listener binds.
- `REQ-MUX-006`: On hosts where a supported TUN backend is available and the process has permission to create/configure TUN devices, including Linux hosts with `/dev/net/tun`, macOS hosts with inline or helper-owned `utun` access, macOS Swift hosts with the packaged helper/XPC boundary, and Windows hosts with a usable WinTun installation, a connected peer shall be able to carry packet traffic between local TUN interfaces over one overlay connection. Symmetric TUN opens for the same interface shall remain routable as channel aliases for one underlying TUN device rather than evicting each other, while a peer-scoped TUN `CLOSE` shall remove its alias from both routing and interface-snapshot indexes. Server-owned shared-TUN listeners shall be reusable across in-process ChannelMux owners rather than reopening duplicate TUN devices for the same prestarted server-owned interface. Peer-scoped traffic accounting shall include TUN payload bytes alongside UDP and TCP payload bytes, and connection snapshots shall distinguish passive/listening TUN interfaces from active TUN packet channels.
- `REQ-MUX-007`: TUN service publication shall use the same peer-scoped catalog and channel-isolation rules as TCP and UDP services, so one peer's TUN interfaces and packet channels do not conflict with another peer's channels or published services. Peer-installed TUN listener startup shall win races with incoming TUN opens for the same target interface so listener hooks still configure the intended service-owned TUN device.
- `REQ-MUX-008`: When a UDP service datagram or TUN packet does not fit into one effective wrapped-session payload budget, the mux layer shall preserve the logical message boundary by fragmenting it across multiple mux messages and reassembling it before local delivery. The protected WebSocket integration contract shall prove this by exact end-to-end payload bytes on both peers rather than by a particular diagnostic-log route.
- `REQ-MUX-009`: The service-definition runtime surface shall accept structured JSON entries for both `own_servers` and `remote_servers`, and a structured service entry may include lifecycle hook commands that execute on listener-side service events (`on_created`, `on_channel_connected`, `on_channel_closed`, `on_stopped`) with placeholder-driven argument/environment rendering. Hook context shall include the configured overlay transport plus the configured and resolved overlay peer endpoint so route-preserving scripts do not need a duplicate peer IP in hook-specific config. The generated local TUN hook environment shall also expose centralized included-route and excluded-route lists, and the effective excluded-route set shall automatically append the resolved active overlay peer IPv4 and IPv6 host routes so full-tunnel clients keep their own overlay transport peer off-tunnel by default. ChannelMux target-side TCP dials default to system proxy resolution through `channel_mux_egress.mode=system` (or nested `channel_mux.egress.mode=system`), reusing the shared HTTP CONNECT tunnel path; system mode uses WinHTTP/PAC/manual endpoint resolution on Windows and `HTTP_PROXY`, `HTTPS_PROXY`, and `NO_PROXY` environment resolution on Linux/POSIX. UDP target dials shall remain direct because the current system-proxy resolver does not provide a UDP relay contract. When the local TUN path applies source normalization for shared/full-tunnel operation, the runtime shall rewrite locally emitted packet sources to the configured tunnel identity addresses unless that normalization is explicitly disabled, and an explicit disable path shall remain observable. The `on_stopped` listener hook shall run before the listener service is closed during overlay disconnect, peer disconnect, catalog replacement, or process shutdown so operator routing/firewall teardown can run while the local service resources still exist. Hook executable paths that include a path separator may be relative to the loaded configuration file directory, while bare command names remain resolved through the process `PATH`. For `tun -> tun` structured services, the `options` object may also carry a static `shared_tun_ownership` definition for future shared server-side packet switching; in the delivered Phase 1 config contract, that ownership definition shall be validated as exact host-address ownership only (`/32` IPv4, `/128` IPv6, or bare host addresses), duplicate address claims shall be rejected at config parse time, and the option shall not be accepted on non-TUN services. In the delivered Phase 2 runtime slice, ChannelMux shall materialize that ownership definition into peer-scoped internal state for TUN services, keep that state synchronized with TUN listener/channel lifecycle and peer disconnect cleanup, and expose the committed ownership plus active peer/channel bindings in TUN connection snapshots without changing baseline 1:1 TUN forwarding semantics. In the delivered Phase 3 anti-spoof slice, when shared-TUN ownership policy is active for a TUN service, inbound peer-to-server TUN packets shall be parsed as IPv4 or IPv6, malformed or unsupported IP headers shall be rejected, the packet source address shall be accepted only when it belongs to the sending peer's committed ownership set, and valid broadcast destinations shall not be rejected merely because the destination is not owned by one peer. Shared-TUN attachment shall remain server-owned: a shared-TUN `OPEN` may bind only to a matching prestarted server-owned TUN service and shall not create that shared TUN listener on demand from the peer side. In the delivered Phase 4 egress-routing slice, packets read from the local/server TUN device under an active shared-TUN ownership policy shall be routed only to the active peer channel or channels selected by destination ownership: unicast packets shall leave only on the one active peer channel that owns the destination address, IPv4 limited broadcast packets shall be replicated only to the active peer channels selected by the shared-TUN binding table, and unknown, unmapped, or inactive destinations shall be dropped deterministically instead of opening or reusing an arbitrary fallback channel.

Implementation note: ChannelMux TCP parity simulations that substitute a local synthetic dial shall force direct egress in the fixture, so host `HTTP_PROXY` or platform system-proxy state cannot change the modeled ChannelMux connection transition. This test boundary does not alter the production `system` egress default.
- `REQ-MUX-010`: On hosts where an iOS packet-tunnel provider is available, a connected peer shall be able to carry packet traffic between `NEPacketTunnelFlow` and remote TUN services using the same peer-scoped ChannelMux service model as Linux and Windows TUN adapters. The effective iOS tunnel IPv4 and IPv6 identity shall be derived from live ObstacleBridge configuration rather than hardcoded app constants, and the provider-applied route set shall support full-tunnel forwarding with loopback exclusions when so configured. `TUN_routing.enabled_on_startup` shall apply on iOS and macOS as it does on Python: it controls only the included route masks while preserving the TUN endpoint, addresses, DNS, excluded routes, packet processing, and verification. While the Network Extension remains connected, the route set shall support a route-only enable/suspend control that preserves the extension lifecycle.

Implementation note: the current `REQ-MUX-009` runtime is state-based rather than phase-based. Hook context prefers the live resolved peer endpoint once the overlay knows which peer is active, connected TUN listeners can replay `on_created` routing hooks after peer discovery, and client hook scripts bring interfaces up safely before full underlay-route knowledge is available. Linux client hooks normalize IPv4-mapped IPv6 peer strings, preserve the original underlay route for excluded-route replay including `gw` / `dev` / `src`, use exact-route snapshotting for excluded-route preservation so connected local subnets do not get rebound to a broader underlay/default route, keep loopback destinations on loopback, wait briefly for the TUN device before applying route changes, tolerate teardown after device disappearance, and install point-to-point default routes with `onlink` where required. macOS inline and helper-backed lanes both exercise the Darwin hook scripts against real elevated `utun` devices, including live route/DNS apply/remove and cleanup on machines that permit those privileged host-network effects; hosted macOS CI may instead treat OS-refused route, address, ping, helper-approval, or empty inline TUN-row side effects as diagnostic while still requiring the admin/runtime diagnostics endpoint to remain responsive and, when a row is exposed, to report the attempted interface and verification state. The same exclusion normalization now also filters IPv4-mapped IPv6 loopback or host forms such as `::ffff:127.0.0.1/128` so dual-stack full-tunnel exclusions do not duplicate self-reachability entries across families. Structured `tun -> tun` services may declare exact-host `options.shared_tun_ownership`; duplicate or non-host claims are rejected at parse time, shared-TUN listeners remain server-owned and prestarted, ownership/binding state is exposed in snapshots with the active bound ownership IPs for each peer binding, inbound anti-spoof checks apply only when that policy is active, and if reconnect or teardown state temporarily drops the inbound channel-owner identity while the source IP still belongs to one committed peer, the runtime shall attempt recovery from the live binding table before dropping the packet and shall emit explicit diagnostics when recovery is not possible. Server-side shared-TUN egress routes only to the designated active peer or peers, and peer-to-peer unicast can be relayed through the server switch without self-looping. When a shared server-owned TUN service is intentionally retained across overlay reconnect or peer disconnect recovery, listener teardown shall not treat that retention as a full adapter stop: reconnect cleanup shall keep the shared device alive, skip `on_stopped` hook execution for the retained adapter instance, and reserve those stop hooks for actual adapter shutdown or catalog removal. The macOS Swift host-runner path mirrors that same adapter-lifecycle contract and drops stale TUN channel bindings on every overlay transport-epoch reset, advances the ChannelMux connection sequence, sends a fresh `OPEN` before `DATA` after a peer restart, and regenerates its remote-service catalog with that same epoch after WebSocket app readiness so the server does not remove the catalog during the TUN reopen. The same destination-routing semantics are defended on the Swift side through the `CryptoKit`-free ChannelMux component parity runner, and the Linux elevated lane proves the rules under real `/dev/net/tun` I/O. The paired `REQ-MUX-010` provider bootstrap path normalizes comma-separated multi-host `udp_peer` values down to one bootstrap host for the native packet-tunnel contract while preserving the full runtime peer-candidate list for Swift UDP fallback.

- `REQ-MUX-011`: When the runtime observes active transport backlog for a session (`buffered_frames > 0` / equivalent waiting-count signal), the ChannelMux shall limit local TUN inflow instead of relying on a fixed transmit-delay cutoff. The limiter shall measure successful local-TUN forwarding volume over `100ms` windows and, while backlog exists, allow at most `90%` of the previous window's delivered byte volume into the next `100ms` window. When backlog is absent, no TUN-specific inflow throttle shall apply. The runtime shall continue to carry other non-TUN services where feasible and shall expose the gating or throttling decision through debug/trace logs, the dedicated TUN/routing admin surface, and peer-scoped metrics.

Implementation note: the current `REQ-MUX-011` runtime keeps stable service-scoped throttle windows for the baseline direct 1:1 TUN path and route-scoped throttle windows for shared-TUN delivery paths. When shared-TUN routing selects one peer/channel set, that route gets its own byte budget and throttle-drop counter, so one backed-up peer path does not consume the budget for another healthy peer path. Shared broadcast delivery also runs in its own throttle scope rather than sharing a unicast peer's budget. Shared-TUN snapshots expose those throttle scopes and per-peer counters for the dedicated TUN/routing admin surface, while `/api/peers` and the peer-connections UI expose the peer-local throttle summary that operators use for troubleshooting. A `CryptoKit`-free Swift/Python ChannelMux component parity runner compares the same multi-scope throttling decisions as the Python runtime.

Implementation note: native Swift UDP, TCP, WebSocket, and QUIC overlay owners regenerate ChannelMux startup control frames from the active connection sequence after app readiness, then proactively allocate and announce the configured local TUN endpoint. Remote-service catalog publication remains a ChannelMux lifecycle operation rather than a transport-specific startup behavior and precedes that TUN `OPEN` in the ordered mux output.

Implementation note: the current Python integration harness now also carries a focused two-client `myudp` listener restart regression for remote TCP reoffer behavior. After one client's remote TCP service is already in use, the listener server may go away and later return on the same overlay port; the server-side peer view must recover to two active peers again, and fresh TCP connects to the same reoffered remote-service ports must succeed without requiring a full manual teardown of the listener process.
Implementation note: the delivered shared-TUN verification follow-up now treats the server-side global-connectivity probe as a virtual shared-TUN peer instead of a privileged bypass around the mux/router path. The probe source address is owned by an explicit local-virtual peer binding, the request enters the same shared-TUN dispatcher path as an ordinary connected peer, and the returning reply is consumed locally instead of being forwarded onward as unassigned shared-TUN traffic. Python and Swift now also keep the higher-layer overlay readiness model aligned: `myudp`, `tcp`, `ws`, and `quic` publish normalized `connection_layers`, and ChannelMux-facing readiness is derived from the top layer's `app_ready` state rather than a transport-only "socket up" signal, so startup probes and mux replay wait for SecureLink authentication when that wrapper is enabled. For SecureLink-backed sessions, ChannelMux now treats only `authenticated` and transport-preserving `reauthenticating` epochs as overlay-inflow-eligible; initial handshakes, failed states, and disconnected lower-layer epochs stay blocked even if the transport socket itself still exists. The current diagnostics slice also emits focused INFO-level ICMP breadcrumbs around local-TUN read, overlay send/receive, and local-TUN write decisions so packet loss can be correlated across both ends without enabling full packet logging.

Implementation note: the live-config derivation for `REQ-MUX-010` now also includes a dedicated `ios_tunnel_network` override block so packet-tunnel route sets, DNS servers, MTU, and dual-stack tunnel identities can be overridden from runtime config without forking app-side constants.


## Loss and delay requirements

- `REQ-MYU-001`: The myudp transport shall continue to function under added propagation delay.
- `REQ-MYU-002`: The myudp transport shall recover from selected DATA frame loss through retransmission.
- `REQ-MYU-003`: The myudp transport shall recover from selected CONTROL frame loss.
- `REQ-MYU-004`: The myudp transport shall correctly transfer large payloads under delayed and lossy conditions.
- `REQ-MYU-005`: Bidirectional myudp traffic shall remain functional when both directions are active concurrently.
- `REQ-MYU-006`: The myudp transport shall tolerate heavy early loss patterns without silently corrupting delivered payloads.
- `REQ-MYU-007`: The myudp transport shall expose an averaged transmit-delay metric for acknowledged `DATA` frames, derived from first-send time minus half of the current RTT estimate, so operators can distinguish payload delivery delay from raw RTT. When an idle RTT refresh updates the active session RTT estimate without a new acknowledged `DATA` frame, the runtime shall re-base `transmit_delay_est_ms` to half of the refreshed RTT estimate so stale delay spikes do not persist indefinitely during idle periods.

Implementation note: the transport-envelope RTT and retransmission details for the delivered `myudp` runtime are documented in [MYUDP_DESIGN.md](/home/ohnoohweh/quicbr_test/docs/MYUDP_DESIGN.md). In particular, retransmission must rebuild a fresh protocol envelope for each actual wire send so `tx_ns` and `echo_ns` reflect the resend attempt rather than a stale raw datagram image.
Implementation note: current focused regression coverage for the `REQ-MYU-*` slice also includes semantic log-replay and transport-edge checks that preserve fresh retransmit frame rebuilding, protect receiver gap state across sender reset, clear stale receiver/control state across full transport-epoch reset, keep log-based repro analysis aligned to the same observed session epoch, keep a frame that was reported missing on a persistent RTT-paced retry path until cumulative ACK progress actually clears that gap, verify that acknowledged `DATA` frames publish an EWMA transmit-delay estimate through the runtime status/dashboard path, and verify that idle RTT refresh rebases the live transmit-delay estimate back to half-RTT when no fresh `DATA` ACK has arrived yet. Python and Swift default the `myudp` send window to `max_inflight=200` unless runtime config explicitly overrides it, while still clamping configured values to the supported session range.

Implementation note: the myUDP2 codec boundary is defined by a bounded `u32` stream-record serializer/deserializer and a strict DATA_BATCH parser/encoder that consumes the frozen wire vectors, rejects invalid record lengths and trailing bytes, and enforces the IPv6-safe batch budget. The Python and shared Swift myudp runtimes carry those stream bytes in DATA_BATCH datagrams, schedule complete chunks within the batch budget, and retransmit each missing chunk in a freshly built batch envelope. The Python E2E harness records sanitized DATA_BATCH framing metadata and qualifies concurrent small-record coalescing plus exact-once recovery after a dropped multi-record batch. `get_stream_record_limit()` is the upper-layer budget contract: ChannelMux uses it for read and fragment sizing, Compression forwards it, and SecureLink reserves protected-frame overhead. The `myudp.budget` peer-status object and transport metrics report stream bytes, chunks, batches, queue bytes/age, retransmitted chunks, malformed batches, and malformed stream records. Swift macOS/iOS device and mixed-runtime qualification remains required before this wire format is eligible for a distributed-network release; there is no runtime wire-format fallback.
Implementation note: stream-style transports that do not maintain a separate ACK-derived payload-delay estimator (`tcp`, `quic`, and `ws`) shall publish `transmit_delay_est_ms` as `rtt_est_ms / 2` on their session metric surface so operator dashboards and ChannelMux policy can consume one consistent delay field across transports.
Implementation note: [ARCHITECTURE.md](/home/ohnoohweh/quic_br/docs/ARCHITECTURE.md) now also defines the overload/freshness policy that future transport work is expected to follow: bounded queues, admission-side shedding for freshness-sensitive datagram traffic, ingress-side backpressure for TCP streams, and observability of queue pressure/drop state rather than unbounded stale backlog growth.

## Admin web requirements

- `REQ-ADM-001`: The admin web interface shall expose health, status, peer, connection, proxy-provider, log, and configuration-related APIs needed for operational visibility. The same operator surface shall remain usable from packaged application builds, including packaged iOS runtimes, so the WebAdmin HTML/JS assets are still served at `/` even when the source-tree-relative `./admin_web` directory is not present at runtime. The repository-top `admin_web/` directory remains the canonical asset source for source-tree workflows, and packaged builds shall stage those same assets into the runtime bundle so hosts such as the iOS Briefcase app can seed or serve WebAdmin without a checked-in duplicate mirror under `src/`. The iOS foreground app shall use the extension's configured loopback Admin API and a native control for Network Extension lifecycle; TUN routing remains controlled through the shared WebAdmin API rather than a second native switch. When the extension is confirmed off, native UI shall replace the embedded WebAdmin view with a compact offline card so stale administration data is not visible; when active, the embedded page and Safari shall use the same shared Admin API and heading.
- `REQ-ADM-002`: When admin authentication is disabled, the admin API shall remain available without login.
- `REQ-ADM-003`: When admin authentication is enabled, protected admin APIs shall remain unavailable until correct authentication completes.
- `REQ-ADM-004`: After correct authentication, the admin API shall become available to that authenticated client.
- `REQ-ADM-005`: Authentication state shall remain isolated per HTTP client session.
- `REQ-ADM-006`: Peer and connection APIs shall reflect connected peers, channel state, and transfer metrics accurately enough for troubleshooting and regression validation, including peer-local listener metrics such as RTT on active accepted peer rows, peer-scoped RX/TX byte totals and byte-per-second rates across UDP/TCP/TUN payloads, archived closed-channel payload totals for the owning peer, configured service names on UDP/TCP/TUN connection rows, requested `remote_servers` TCP/UDP listener ports as client-role listening rows while the overlay is connected, peer-scoped throttle state where local ingress shedding is active, TUN interface state for passive/listening interfaces and active packet channels, logical TUN connection rows that collapse multiple internal channel aliases for the same underlying TUN device, observable time-since-last-received-frame diagnostics such as `last_incoming_age_seconds` on non-listening peer rows when inbound traffic has been observed, configured-peer endpoint visibility for connecting wrapped client rows before a live peer socket is learned, and the server-reflected `observed_public_ip` for an active client peer. Wrapper layers used to supply that field shall preserve the inner transport's connected state and metrics so the API does not report a live peer as disconnected. The field shall be empty until the current transport epoch receives a valid peer-address reply and shall identify the IPv4 or IPv6 address the connected peer observed, rather than a host-wide external lookup. The API shall also provide a dedicated TUN/routing admin surface for shared-TUN ownership, active routing state, interface-facing ChannelMux flow counters, bounded shared-TUN drop counters with per-reason summaries, a bounded recent-drop buffer with latest-drop context, helper lifecycle phase, helper effective process identity, and runtime warning state such as a local TUN device missing its expected configured tunnel address. A process-shared TUN shall appear there as one physical-interface row with overall traffic derived from its configured peer bindings, rather than one accumulated row per transport holder. For a shared-TUN server, the detail surface shall render one row per configured ownership peer, showing configured and learned addresses, bound/not-bound state, the protocol-qualified connection ID, and binding-scoped RX/TX bytes and packets; connected overlays without a matching binding shall remain explicitly visible as unassigned and not bound. That dedicated TUN/routing surface shall also expose a three-part verification view for configuration, peer-side TUN connectivity, and configurable global connectivity. The global-connectivity verification row shall additionally expose the configured reachability host together with name-resolution status, resolved IP, and resolution-detail text so operators can tell whether a failure is DNS-stage or reachability-stage. When helper mode is active, the helper identity shall report the helper process user; when inline mode is active, the same status surface shall fall back to the main ObstacleBridge process user so operators can distinguish privileged and non-privileged TUN ownership paths at a glance. Blocking peer/global verification probes shall not run inline inside the timeout-sensitive snapshot builder on the Admin server thread; instead the API may return a `pending` or short-lived cached verification result together with freshness metadata such as `cached`, `stale`, and `refresh_in_flight` while a background refresh performs the probe. `/api/status` shall remain limited to runtime metadata, aggregate UDP/TCP/TUN open-connection counts, and aggregate secure-link reload/apply summaries rather than duplicating peer-scoped connection, traffic, compression, throttle, or decode-error fields.

Implementation note: the Peer-page Transport layout keeps Transport State, Reconnect, and Resolved Peer on its first line. Its second line uses one-quarter width each for Connection Uptime and Last Incoming, and one-half width for Own Public IP.

Implementation note: shared-TUN bindings use a process-wide identity composed from the owning ChannelMux holder and its mux-local peer id. This prevents equal peer ids from independent WS and myUDP listener muxes from being conflated; local-TUN replies route through the holder that owns the selected binding, and the Admin snapshot resolves that identity back to the holder’s transport-qualified connection id. Disconnect cleanup removes that identity and binding state from every holder of the shared device. RX/TX counters are mirrored across device holders so either holder supplies an observationally consistent Admin snapshot.

Implementation note: the connected/disconnected pills for Transport State, Protocol Status, and layer Reported Status use green for `connected` and red for `disconnected`; transitional states retain their neutral or progress color.

Implementation note: when a dual-stack listener reports an IPv4 client as an IPv4-mapped IPv6 socket address such as `::ffff:198.51.100.44`, the peer-address protocol shall publish the canonical IPv4 address so Own Public IP matches the IPv4 resolved peer. Own Public IP shall also include the peer-observed outgoing source port, with IPv6 rendered as `[address]:port`, so operators can match the client transport tuple on the server peer list.

Implementation note: the delivered 2026-06-04 shared-TUN admin follow-up now keeps the Swift and Python admin surfaces aligned at the component level as well as the browser level. The shared Swift admin API exposes `/api/tun-routing/status` and the `tun_routing` live topic, native providers derive that payload from their connection snapshot through the same shared helper, and a Linux-runnable Swift/Python Admin Web component parity runner now compares Admin UI payloads, security-advisor payloads, derived TUN/routing payloads, and shared admin API route/topic dispatch without relying on the full macOS host-runner path. The delivered 2026-06-08 Python follow-up now also exposes the runtime-effective included/excluded route lists through `/api/tun-routing/status`, including auto-appended overlay-peer exclusion routes such as the active WS peer `/32`, and the shared browser frontend prefers those runtime values over the static `/api/config` snapshot when rendering the `TUN / Routing` route summary. The delivered TUN verification path now uses platform-appropriate interface/address probes and non-blocking verification refresh: Linux uses `ip` plus internally injected ICMP/ICMPv6 echo probes, macOS Python uses `ifconfig` plus the same internal echo-probe path, macOS Swift uses the shared internal ICMP/ICMPv6 echo-probe path, and iOS reports Network Extension tunnel settings together with the same internal peer/global ICMP verification method through a background-refreshed cache rather than relying on external shell ping. The delivered 2026-06-23 shared-TUN diagnostics follow-up now also exposes the effective bound ownership IP or IPs for active shared-TUN peer bindings so the WebAdmin peer/routing view can show which address claim is linked to the current binding rather than only showing the peer id. Current Python and Swift peer snapshots select `last_incoming_age_seconds`, `rtt_est_ms`, and `transmit_delay_est_ms` from the active transport runtime when the top-level active peer row is unset, so `myudp`, `tcp`, `quic`, and `ws` rows keep the operator metrics visible through `/api/peers` and live peer snapshots. Passive listener rows remain the exception: they preserve explicit null transport metrics rather than inheriting session fallback values, so a zeroed listening row stays distinguishable from an active accepted peer row. For malformed or junk `myudp` listener traffic that creates a transient `connecting` peer row, `/api/peers` is allowed to publish that row before the `decode_errors` counter increments on the same row; regression checks for this slice shall therefore wait for the nonzero `decode_errors` signal before asserting the stale-row reap behavior. The delivered iOS remote-admin regression slice also requires the forwarded admin TCP listener to carry `/api/status`, `/api/live`, and the WebAdmin asset set without blocking the packet-tunnel bridge queue behind verification-probe waits, so the iPhone WebAdmin surface stays responsive while TUN verification refresh runs in the background.
Implementation note: the current support-diagnostics follow-up keeps `/api/status` intentionally lightweight while still surfacing the operator data needed to debug shared-TUN health from the summary view. Python and Swift now both include the effective TUN interface name, helper runtime interface name, peer/global ICMP stage counters, probe-boundary counters, local-reply stage counters, and last timeout diagnostics in the status payload, while the dedicated TUN/routing endpoint remains the detailed source for peer-scoped ownership, drop summaries, and verification rows. The same slice also requires helper-backed shared-TUN ownership handoff to cancel the previous helper-read loop before a new mux owner starts consuming replies, and it suppresses local-virtual verification probes on inactive transports so disconnected wrapped paths do not emit false timeout evidence while another authenticated path is active.
Implementation note: the delivered layered-readiness parity follow-up now keeps `/api/peers`, `/api/status`, the Swift host runner, and the iOS packet-tunnel admin surface aligned on the same readiness model. Swift overlay owners expose `connection_layers` and top-layer `app_ready` alongside the transport snapshot, and admin status uses that top-layer readiness for connected/disconnected reporting instead of inferring health from raw transport-up state alone. The shared ChannelMux local-TUN admission boundary, iOS packet pump, and internal ICMP verification path shall reject packets before that connected state; probe target name resolution is likewise skipped before admission, and all paths become eligible immediately once the state is reached. macOS Swift elevated packet-carry validation also waits for the configured peer route to resolve through the created `utun` before injecting traffic, so it tests packet delivery only after the local network contract is observable. While local TUN throttling is active, both Python and Swift internal connectivity probes shall likewise suspend hostname resolution and ICMP injection so diagnostics do not compete with forwarded traffic.
For reconnect-capable Python client transports (`tcp`, `quic`, and `ws`), connecting peer rows now also publish the remaining time until the next address/reconnect attempt, and when `client_restart_if_disconnected` is enabled they publish the remaining time until the runner-side restart watchdog will escalate the outage into a full runtime restart. The WebAdmin peer details view renders those countdowns as `Next Address Attempt` and `Restart In` so operators can tell whether the runtime is still retrying the current transport path or is about to hand recovery off to a restart.

Implementation note: during a delayed launcher restart, the launcher-owned `/api/status` response represents remaining restart time as a negative `uptime_sec`. WebAdmin renders that signed value as a countdown and keeps the normal page loaded until the supervised runtime returns.

Implementation note: accepted listener-peer snapshots retain child-session metrics but derive SecureLink and compression layer status from the peer's wrapper diagnostics, so an authenticated, active peer cannot report disconnected layer pills.


- `REQ-ADM-007`: Secret configuration keys exposed by the runtime (for example `secure_link_psk` and `admin_web_password`) shall be writable through the admin configuration update API but must never be returned in cleartext by read-only snapshots. The admin UI shall render these keys as password-style inputs (empty on read) and must not display the stored secret value through the normal configuration row or editor.

- `REQ-ADM-008`: When admin authentication is enabled, configuration updates submitted through WebAdmin shall require a fresh challenge-response confirmation bound to the exact update block before the runtime applies the change. The confirmation proof shall be derived from a server-issued seed, the current admin password, and the canonicalized update payload so the proof cannot be reused for a different configuration block. The guarded write flow shall cover both secret and non-secret configuration changes rather than protecting only password-like fields.

- `REQ-ADM-009`: Runtime configuration shall be loaded from registered configuration sections only, and saved configuration files shall contain those sections only. Unsectioned and unregistered values shall not alter runtime configuration. Saved files shall not expose `admin_web_password` or `secure_link_psk` in plaintext. When the runtime loads an encrypted configuration file, it shall restore those values back into memory so the application can continue to start, save, and operate with the configured secrets. The encryption/decryption key derivation for those saved secrets shall come from machine-derived identity only; runtime environment-variable overrides shall not be accepted for cross-machine secret portability.
- `REQ-ADM-010`: The admin web onboarding surface shall expose APIs to derive connection profiles from current runtime/config state, generate invite tokens, and preview invite tokens before apply. Invite preview output shall not expose the secure-link PSK in plaintext while the apply/update payload keeps the effective secure-link setting values usable for runtime configuration updates. Invite-apply updates shall accept the same grouped configuration section shape used by persisted runtime config, including grouped `TUN_routing` updates, rather than requiring callers to flatten those sections manually. Invite tokens shall carry the peer setup fields needed to recreate the effective onboarding config, including admin web port, compression settings, TUN routing overrides, mux TCP backpressure knobs, proxy-provider settings, own/remote service catalogs, and secure-link mode/PSK material.
- `REQ-ADM-011`: The WebAdmin configuration editor shall make structured `own_servers` and `remote_servers` service catalogs editable without requiring operators to hand-edit the raw JSON for routine changes. The service editor shall preserve the same JSON config shape on save, show the current JSON value as the row preview, and provide a focused per-service popup flow for adding, removing, and navigating service entries. When an operator removes one service while editing a populated catalog, the editor shall commit that removal without resurrecting the deleted draft from stale modal form state, and when another service remains, the popup shall stay open on the next valid service entry so repeated cleanup does not require reopening the editor after each delete.
- `REQ-ADM-012`: The WebAdmin configuration editor may provide a deliberate reveal action for `secure_link_psk`, but that action shall require a fresh admin-password challenge-response proof before the server returns any reveal payload. The reveal payload shall not contain the plaintext PSK; it shall contain an encrypted envelope that the browser decrypts locally with the re-entered admin password, and the UI shall display the decrypted value only inside the reveal popup until that popup is closed. When the browser cannot perform the local decrypt step because remote plain HTTP is not a secure context, the UI shall present an explicit explanation before password entry and recommend the own-server/server-role overlay path ahead of generic tunnel alternatives.

- Implementation note: the admin web challenge-response login shall remain usable over plain HTTP as well as HTTPS. When the page is not in a secure context, the browser-side proof generation shall fall back to an equivalent client-side SHA-256 implementation so the login flow still works without requiring `window.crypto.subtle`.

Development-process measures such as test-execution discipline, regression-writing policy, and CI split strategy are documented in [DEVELOPMENT_PROCESS.md](/home/ohnoohweh/quic_br/docs/DEVELOPMENT_PROCESS.md). They intentionally do not appear here because they govern how the project is built and validated, not what the delivered project promises to an operator.

Repository governance update (process change): the project now documents and requires a consistent PR style and a repository PR template to improve review quality and traceability. See [DEVELOPMENT_PROCESS.md](/home/ohnoohweh/quic_br/docs/DEVELOPMENT_PROCESS.md) and `.github/PULL_REQUEST_TEMPLATE.md` for the required PR structure and checklist. This administrative change is intended to improve reviewer efficiency and traceability when implementation, tests, or architecture documents are modified. The top-level [README.md](/home/ohnoohweh/quic_br/README.md) is intentionally treated as a compact entrypoint and coverage snapshot; detailed requirement, design, system-boundary, and test-catalog narrative belongs in the dedicated docs rather than being duplicated in that snapshot. When behavior, tests, or architecture/process guidance changes, the snapshot is expected to stay current while the durable explanation remains in [REQUIREMENTS.md](/home/ohnoohweh/quic_br/docs/REQUIREMENTS.md), [ARCHITECTURE.md](/home/ohnoohweh/quic_br/docs/ARCHITECTURE.md), [SYSTEM_BOUNDARY.md](/home/ohnoohweh/quic_br/docs/SYSTEM_BOUNDARY.md), and [README_TESTING.md](/home/ohnoohweh/quic_br/docs/README_TESTING.md).

Repository governance update (testability): the unit-test traceability snapshot now includes focused unit coverage for the remaining requirement IDs that were previously defended only by integration tests. The added unit coverage protects Admin Web authentication gate behavior, accepted-peer reporting identity across listener transports, UDP IPv4/IPv6 peer labeling, and core `myudp` reliability/session invariants while preserving the integration suite as the full black-box signal for end-to-end overlay behavior.

Repository governance update (public endpoint privacy): documentation, design examples, and committed test fixtures use IANA-reserved documentation address ranges rather than live operator or deployment endpoints. This keeps routing and address-normalization examples reproducible without publishing infrastructure addresses.

The supporting product-requirement traceability manifest is maintained in [.github/requirements_traceability.yaml](/home/ohnoohweh/quic_br/.github/requirements_traceability.yaml). It is stored with the repository's CI/support metadata rather than in `docs/`, but it continues to trace these product requirements to their defending tests.
