#!/bin/sh
set -eu

if ! command -v python3 >/dev/null 2>&1; then
    echo "ObstacleBridge requires python3 on the Synology host." >&2
    exit 1
fi

python3 - <<'PY'
import sys
if sys.version_info < (3, 8):
    raise SystemExit("ObstacleBridge requires Python 3.8 or newer.")
PY
